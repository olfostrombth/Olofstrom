<p id="notice"><%= notice %></p>

<p>
  <%= @step.category_id %>
  <strong>Name:</strong>
  <%= @step.name %>
</p>

<p>
  <strong>Desc:</strong>
  <%= @step.desc %>
</p>

<p>
  <strong>Category:</strong>
  <%= @step.category %>
</p>

<button id="video_toggle" class="button success radius">Lägg till video</button>
<div class="video_form">
  <%= render '/videos/form' %>
</div>
<br>
<button id="guide_toggle" class="button success radius">Lägg till guide</button>
<div class="guide_form">
  <%= render '/guides/form' %>
</div>
<br>
<button id="assignment_toggle" class="button success radius">Lägg till uppdrag</button>
<div class="assignment_form">
  <%= render '/assignments/form' %>
</div>
<br>
<button id="quiz_toggle" class="button success radius">Lägg till quiz</button>
<div class="quiz_form">
  <%= render '/quizzes/form' %>
</div>
<br>

<% @step_items.each do |item| %>
   <%= item['name'] %>
        <% if item['desc'] %>
            <%= item.desc %>
        <% end %>

         <% if item['url'] %>
             <%= item.url %>
         <% end %>
<% end %>



<br>
<%= link_to 'Edit', edit_step_path(@catname,@step.name) %> |
<%= link_to 'Back', steps_path %>
