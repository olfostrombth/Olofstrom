<p id="notice"><%= notice %></p>

<p>
  <strong>Titel:</strong>
  <%= @assignment.name %>
</p>

<p>
  <strong>Uppdrag:</strong>
  <%= @assignment.desc %>
</p>

<%#= Will need module and step it belongs to, so that it can change the value of the json %>
<p>
  <strong>Klar:</strong>
  <input type="checkbox" id="assignment_id" name="puppy[gooddog]" value="yes" />
</p>
<%= link_to 'Edit', edit_assignment_path(@assignment) %> |
<%= link_to 'Back', assignments_path %>
