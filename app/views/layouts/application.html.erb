<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title><%= @title ? @title: "Olofström  - OKIT" %></title>

  <%= stylesheet_link_tag    "application" %>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  <%= javascript_include_tag "vendor/modernizr" %>
  <%= Gon::Base.render_data({}) %>
  <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  <%= javascript_include_tag "//cdnjs.cloudflare.com/ajax/libs/typeahead.js/0.9.3/typeahead.min.js", 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= favicon_link_tag '../favicon.ico' %>
</head>

<body>

<div class="row container">
  <div class="large-12 columns wrapper">
    <% if current_user %>
        <div class="small-12 medium-2 large-2 columns top"><%= link_to image_tag("logo.png").html_safe, root_path %></div>
        <div class="medium-2 large-2 columns sidebar">


          <a href="#"><div class="small-12 medium-12 large-12 columns side"><p><i class="fa fa-user"></i><%= current_user.name %></p></div></a>
          <div class="small-12 medium-12 large-12 columns side arch-toggle"><p><i class="fa fa-trophy"></i>Badges</p></div>
          <div class="large-12 columns arch">
            <div class="row">
              <h4>FÄRDIGA</h4><br>
              <div class="small-6 large-6 columns"><%= image_tag("badge.png") %></div>
              <div class="small-6 large-6 columns"><%= image_tag("badge.png") %></div>
              <div class="small-6 large-6 columns"><%= image_tag("badge.png") %></div>
              <div class="small-6 large-6 columns"><%= image_tag("badge.png") %></div>
              <div class="small-6 large-6 columns"><%= image_tag("badge.png") %></div>
              <div class="small-6 large-6 columns"><%= image_tag("badge.png") %></div>
            </div>
          </div>
        <%= link_to '<div class="small-12 medium-12 large-12 columns side"><p><i class="fa fa-sign-out"></i>Logga ut</p></div>'.html_safe, signout_path, id: "sign_out" %>
        
          <a href="#"><div class="small-12 medium-12 large-12 columns side fed"><p><i class="fa fa-comment-o"></i>Flöde</p></div></a>
          <div class="large-12 columns fed-toggle">
            <div class="row">
              <div class="small-6 large-6 columns flod">
          <% @activities = get_activities.first(6) %>
            <% @activities.each do |activity| name = activity.owner.name.split(" ")
            link = activity.key.split(">")
            nar = link[1].split("<") %>
            <% title = nar[0].truncate(15)
            linkx = link.split(": ") %>
            <% linkz = linkx[0][0].split(": ") %>
            <%#= linkz %>
            <% l = linkz[1]+">"+title+"</a>" %>
          <div class="large-12 columns avtxt"><p><%=link_to name[0], session_path(activity.owner) %>
               <%= linkz[0] + ": "%><%= l.html_safe %></p></div>
           <% end %>
              </div>
            </div>
          </div>
         
         <!-- <div class="medium-12 large-12 columns side search-side">

            <li class="has-form">
              <div class="row">
                <div class="
                searchforms">
                  <div class="row collapse">
                    <div class="small-12 columns">
                      <%= text_field_tag :query, params[:query], class: "form-control", id: "user_search", autocomplete: "on" %>
                      <%= form_tag search_path, class: "form-inline", method: :get do %>

                    </div>
                    <div class="small-12 columns">
                     <div class="input-group-btn ease"> <%= submit_tag 'sök' %></div>
                     <% end %>
                   </div>
                 </div>
               </div>
             </div>
           </li>

         </div>-->


        <!-- <div class="medium-12 large-12 columns feed">
          <p>Senaste aktiviteter</p>
        </div>

         <div class="large-12 columns feed">
          <% @activities = get_activities.first(5) %>
            <% @activities.each do |activity| name = activity.owner.name.split(" ")
            link = activity.key.split(">")
            nar = link[1].split("<") %>
            <% title = nar[0].truncate(15)
            linkx = link.split(": ") %>
            <% linkz = linkx[0][0].split(": ") %>
            <%#= linkz %>
            <% l = linkz[1]+">"+title+"</a>" %>
          <div class="large-12 columns avtxt"><p><%=link_to name[0], session_path(activity.owner) %>
               <%= linkz[0] + ": "%><%= l.html_safe %></p></div>
               <hr>
           <% end %>
         </div>-->
       </div>

     <div class="small-12 columns mobile-menu-btn"><i class="fa fa-bars fa-3x"></i></div>


        <div class="medium-10 large-10 columns content in">

    <% end %>
    <%= yield %>


    </div>
  </div>
</div>
</div>



</body>

</html>

