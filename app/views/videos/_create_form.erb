<%= form_for(@video) do |f| %>

    <% if @video.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@video.errors.count, "error") %> prohibited this video from being saved:</h2>

          <ul>
            <% @video.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :Namn %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :URL %><br>
      <%= f.text_field :url %>
    </div>
    <div class="field">
      <div class="field">
        <%= f.hidden_field :step_id %>
      </div>
      <div class="field">
        <%= f.hidden_field :typex, value: "video" %>
      </div>
      <div class="field">
        <%= f.hidden_field :row_order_position, value: position %>
      </div>
    </div>
    <div class="actions">
      <%= f.button :Skapa, :class => 'form-btn ease' %>
    </div>
<% end %>
