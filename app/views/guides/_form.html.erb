<%= form_for(@guide, as: :guide, url: guide_path(@guide.name)) do |f| %>

    <% if @guide.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@guide.errors.count, "error") %> prohibited this guide from being saved:</h2>

          <ul>
            <% @guide.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :Namn %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :Text %><br>
      <%= f.text_area :desc, :class => "tinymce" %>
    </div>
    <div class="field">
      <%= f.hidden_field :typex, value: "guide" %>
    </div>
    <div class="field">
      <%= f.hidden_field :step_id %>
    </div>
    <div class="actions">
      <%= f.button :Spara, :class => 'form-btn ease' %>
    </div>
<% end %>
<%= tinymce %>

